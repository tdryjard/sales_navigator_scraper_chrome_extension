import React from "react";
import ReactDOM from "react-dom";

ReactDOM.render(
  <React.StrictMode>
      <p>
        Chrome extension
      </p>
  </React.StrictMode>,
  document.getElementById("root")
);
